// sandbox-game.ts wires itself up at module-load time (event listeners,
// rAF loop, settings + audio bootstrapping). Importing it for side
// effects is the entire entry point.
import "./sandbox-game";
